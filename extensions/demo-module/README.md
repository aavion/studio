# Demo Module

Small inactive demo module used by local extension discovery and Admin Extension Management views.

When activated, it contributes a public demo route and one extension setting through `extension.php`.
