# Theme

Theme discovery, template resolution, asset resolution, and override handling live here.

Theme code should validate manifests and overrides before activation, and invalid packages should not block discovery of valid packages.
