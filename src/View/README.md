# View

Shared view context, template helper, macro registry, and safe rendering helpers live here.

Package discovery and lifecycle live in Core package services. View helpers should stay package-scope aware but must not activate package code by themselves.
