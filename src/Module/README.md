# Module

Plugin module manifests, discovery, lifecycle, dependency checks, and module configuration live here.

Module behavior should integrate through documented observe, extend, or replace extension points.
