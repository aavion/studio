# Demo Module

Small inactive demo module used by local package discovery and Admin Package Management views.

When activated, it contributes a public demo route and one package setting through `package.php`.
