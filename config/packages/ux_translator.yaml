ux_translator:
    # The directory where the JavaScript translations are dumped
    dump_directory: '%kernel.project_dir%/var/translations'

when@prod:
    ux_translator:
        # The project currently uses JavaScript with AssetMapper; skip TypeScript type dumps during production warmup.
        dump_typescript: false
