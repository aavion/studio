{% extends '@backend/admin.html.twig' %}

{% set theme_sections = themes() %}

{% block title %}{{ 'admin.themes.title'|trans }}{% endblock %}

{% block admin_sidebar %}
    <nav aria-label="{{ 'ui.admin.navigation'|trans }}">
        {% include '@backend/partials/navigation/_tree.html.twig' with {items: navigation} only %}
    </nav>
{% endblock %}

{% block admin_body %}
    {% include '@backend/admin/partials/_page-header.html.twig' with {
        eyebrow: 'ui.admin.area'|trans,
        title: 'admin.themes.title'|trans,
    } only %}

    {% for section in theme_sections %}
        <section class="system-panel system-backend-theme-overview" data-theme-section="{{ section.key }}">
            <h2>{{ section.title_key|trans }}</h2>

            {% if section.themes|length > 0 %}
                <div class="system-backend-theme-grid">
                    {% for theme in section.themes %}
                        <article {{ html_attributes({
                            class: theme.immutable ? 'system-backend-theme-card is-immutable' : 'system-backend-theme-card',
                            'data-extension-name': theme.extension_name,
                            'data-immutable': theme.immutable ? 'true' : null,
                            'data-theme-status': theme.status,
                        }) }}>
                            {% set theme_version = theme.version|default(null) %}
                            <header class="system-backend-theme-card-header">
                                <h3>
                                    <strong>
                                        <a href="{{ theme.detail_path }}">{{ theme.label_key ? theme.label_key|trans : theme.label }}</a>
                                    </strong>
                                    {% if theme_version %}
                                        <span class="system-muted">{{ theme_version }}</span>
                                    {% endif %}
                                </h3>
                            </header>

                            <div class="system-backend-theme-preview" aria-hidden="true"></div>

                            <footer class="system-backend-theme-card-footer">
                                {% if theme.quick_action %}
                                    {% if theme.quick_action.disabled %}
                                        <span class="system-backend-button system-button-{{ theme.quick_action.variant }}" aria-disabled="true">
                                            {{ theme.quick_action.label_key|trans }}
                                        </span>
                                    {% else %}
                                        <a class="system-backend-button system-button-{{ theme.quick_action.variant }}" href="{{ theme.quick_action.path }}">
                                            {{ theme.quick_action.label_key|trans }}
                                        </a>
                                    {% endif %}
                                {% endif %}
                            </footer>
                        </article>
                    {% endfor %}
                </div>
            {% else %}
                <p class="system-muted">{{ 'admin.themes.empty'|trans }}</p>
            {% endif %}
        </section>
    {% endfor %}
{% endblock %}
