<header class="studio-page-header">
    <div>
        <div class="studio-kicker">{{ eyebrow|default('ui.setup.area'|trans) }}</div>
        <h1>{{ title }}</h1>
        {% if subtitle is defined and subtitle is not empty %}
            <p class="studio-muted">{{ subtitle }}</p>
        {% endif %}
    </div>
</header>
