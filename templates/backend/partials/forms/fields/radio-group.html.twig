{% set field_id = id|default(name|default('field')) %}
<fieldset id="{{ field_id }}" class="studio-backend-choice-group">
    <legend class="studio-backend-form-label">{{ label }}</legend>
    {% for option_value, option_label in options|default({}) %}
        {% set option_id = field_id ~ '-' ~ loop.index %}
        <label class="studio-backend-radio-label" for="{{ option_id }}">
            <input
                id="{{ option_id }}"
                class="studio-backend-radio"
                type="radio"
                name="{{ name }}"
                value="{{ option_value }}"
                {% if option_value == value|default(null) %}checked{% endif %}
                {% if required|default(false) %}required{% endif %}
            >
            <span>{{ option_label }}</span>
        </label>
    {% endfor %}
    {% include '@backend/partials/forms/_help.html.twig' with {help: help|default(null)} only %}
    {% include '@backend/partials/forms/_errors.html.twig' with {errors: errors|default([])} only %}
</fieldset>
