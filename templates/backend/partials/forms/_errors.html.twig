{% if errors|default([]) is not empty %}
    <ul class="studio-backend-form-errors">
        {% for error in errors %}
            <li>{{ error|trans }}</li>
        {% endfor %}
    </ul>
{% endif %}
