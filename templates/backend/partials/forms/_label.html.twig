<label class="system-backend-form-label"{% if id|default(null) %} for="{{ id }}"{% endif %}>
    {{ label }}
    {% if required|default(false) %}
        <span class="system-form-required" aria-label="{{ 'ui.form.required'|trans }}">*</span>
    {% endif %}
</label>
