{% set copyright = footer_copyright(area) %}
{% if copyright is not empty %}
    <footer class="system-footer system-footer-{{ area }}">
        {{ copyright|render_markdown('basic')|raw }}
    </footer>
{% endif %}
