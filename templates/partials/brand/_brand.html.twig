<a class="{{ class|default('system-brand') }}" href="{{ href|default(path('content_home')) }}">
    <span class="system-brand-mark" aria-hidden="true">a</span>
    <span class="system-brand-name">{{ name|default('ui.app.name'|trans) }}</span>
</a>
