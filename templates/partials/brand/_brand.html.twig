<a class="{{ class|default('studio-brand') }}" href="{{ href|default(path('content_home')) }}">
    <span class="studio-brand-mark" aria-hidden="true">a</span>
    <span class="studio-brand-name">{{ name|default('ui.app.name'|trans) }}</span>
</a>
