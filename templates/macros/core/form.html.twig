{% macro field_row(label, control, help = null, errors = []) %}
    <div class="system-form-row">
        <label class="system-form-label">{{ label }}</label>
        <div class="system-form-control">{{ control|raw }}</div>
        {% if help %}
            <p class="system-form-help">{{ help }}</p>
        {% endif %}
        {% if errors is not empty %}
            <ul class="system-form-errors">
                {% for error in errors %}
                    <li>{{ error }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
{% endmacro %}
