{% set field_id = id|default(name|default('field')) %}
{% set selected_values = values|default([]) %}
<fieldset id="{{ field_id }}" class="studio-choice-group">
    <legend class="studio-form-label">{{ label }}</legend>
    {% for option_value, option_label in options|default({}) %}
        {% set option_id = field_id ~ '-' ~ loop.index %}
        <label class="studio-checkbox-label" for="{{ option_id }}">
            <input
                id="{{ option_id }}"
                class="studio-checkbox"
                type="checkbox"
                name="{{ name }}[]"
                value="{{ option_value }}"
                {% if option_value in selected_values %}checked{% endif %}
            >
            <span>{{ option_label }}</span>
        </label>
    {% endfor %}
    {% include '@frontend/partials/forms/_help.html.twig' with {help: help|default(null)} only %}
    {% include '@frontend/partials/forms/_errors.html.twig' with {errors: errors|default([])} only %}
</fieldset>
