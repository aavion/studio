# Package asset imports

This directory is managed by the package lifecycle.

Only assets from mirrored active packages are included here so AssetMapper can serve them from the normal `assets/` tree. Source package directories under `packages/` are not exposed wholesale.
