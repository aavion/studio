# Extension assets

This directory is managed by the extension lifecycle.

Only assets from active extensions are mirrored here so AssetMapper can serve them from the normal `assets/` tree. Source extension directories under `extensions/` are not exposed wholesale.
