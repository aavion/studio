# Extension asset imports

This directory is managed by the extension lifecycle.

Only assets from mirrored active extensions are included here so AssetMapper can serve them from the normal `assets/` tree. Source extension directories under `extensions/` are not exposed wholesale.
