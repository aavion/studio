# Package assets

This directory is managed by the package lifecycle.

Only assets from active packages are mirrored here so AssetMapper can serve them from the normal `assets/` tree. Source package directories under `packages/` are not exposed wholesale.
